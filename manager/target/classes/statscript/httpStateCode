#!/bin/awk -f 
BEGIN{
	OFS=" : ";
}
{
	split($5,array," : ");
	stateCode=array[2];
	stateCodes[stateCode]++;
	if(stateCode>=400){
		print $4,$5,$7 > efile;
	}
}END{
	for(code in stateCodes){
		print(code,stateCodes[code]);
	}
}